# webserver

Tiny webserver in C that serves static files. It supports GET, HEAD, OPTIONS and TRACE. It also supports Connection: keep-alive.

The purpose of this tiny webserver is just for quick dev/test of html and javascript locally. It is not intended for any production use whatsoever.

## Mac/Linux

Compile with make/gcc

## Windows

Build with Visual Studio or via cl.exe on command line

